// STANDALONE BUILD STUB — swapped in for src/github-sync.ts by
// scripts/package-studio-to-go.sh. Self-host has no GitHub document sync, so
// pushing a document upstream is a no-op. The signature matches the cloud module
// so documents.ts compiles unchanged. The real GitHub App / Octokit source is
// never shipped.

export async function pushDocument(_workspaceId: string, _documentId: string): Promise<void> {}
